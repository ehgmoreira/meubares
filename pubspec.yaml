name: meubar
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.8.0  # Dependência para Firebase
  cloud_firestore: ^4.17.5  # Adicionando a dependência do Firestore
  google_maps_flutter: ^2.3.0  # Dependência para Google Maps
  cupertino_icons: ^1.0.8  # Dependência para ícones do Cupertino

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0  # Dependência para melhores práticas de codificação

flutter:
  uses-material-design: true